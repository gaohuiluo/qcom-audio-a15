<thinking>针对高通Android15上的AudioReach架构的学习，将由我先梳理其背景、普及历程及在Android15上的状态。AudioReach是高通推出的新一代端到端统一音频处理框架，可整合原有APM、QACT等音频模块，支持动态加载、多设备适配与低延迟、空间音频等新特性。Android15已强制要求采用AIDL Audio HAL，后续我将按核心概念、分层细节、资料来源、学习路径的逻辑整理学习内容。高通官方资料是最权威的学习来源。我正梳理AudioReach的官方权威资料，目前已明确其核心架构分层：上层为谷歌原生的Android标准Audio Framework；HAL层为高通实现的ARHAL，替代了旧版legacy HIDL/legacy Audio HAL，对接谷歌标准AIDL接口；内核层包含ALSA/ASOC驱动、GPR通用包路由等高通自研的IPC通信机制，ADSP侧的核心处理框架为SPF。

AudioReach的DSP侧核心为AR SPF（Signal Processing Framework），负责加载各类音频处理模块、管理数据流路由与资源调度，同时整合了原APM的功能。它支持模块动态加载、音频通路拓扑动态配置，无需重新刷写ADSP固件即可调整音频处理链路。AudioReach采用Graph（处理图）的核心设计，每个音频流对应一张包含多个处理模块的图，模块间通过端口连接实现数据流动。
目前可获取的官方公开资料包括：高通QDN平台上的AudioReach系列官方文档、Linaro Connect 2023/2024的相关技术演讲，以及Android Partner Summit 2023/2024的相关分享（部分内容可能需要合作伙伴权限）。

高通开发者峰会相关音频主题分享可作为补充参考资料，接下来我会优先梳理Android官方AIDL Audio HAL及整体音频架构文档，明确ARHAL的定位。

我整理了学习AudioReach架构所需的全链路公开开源资源，所有内容均可通过公开渠道获取，无需额外权限：首先是高通官方AudioReach的相关代码均托管在CodeLinaro平台，其中ARHAL为实现AIDL音频接口的用户空间HAL实现，配套的Graph Service Layer（GSL）、通用工具库等核心依赖也有对应公开仓库；内核侧的音频相关驱动、GPR、QRTR、FastRPC等核心IPC与通信模块的代码也可在对应平台的公开内核仓库中获取；另外AOSP也提供了Android15要求的AIDL音频接口定义，方便对照参考。需要说明的是，DSP侧的ADSP固件主体为闭源，仅部分接口定义与头文件公开可查。

除了官方开源代码，第三方学习资料也适合作为入门补充，国内技术社区有不少开发者梳理的架构分析、流程讲解类内容，国外也有相关技术博客、问答社区的参考资料，Linaro还和高通合作推进AudioReach代码的上游合并。

我还补充了两类AudioReach学习资料：一是高通正将其内核驱动提交上游Linux内核，相关设计文档可在指定上游社区搜索获取；二是B站、YouTube等平台有相关的架构讲解视频，可作为补充学习资源。

为了方便学习者对比掌握，我梳理了AudioReach与老音频架构的核心差异，明确了老架构的划分标准、各层级特点及配套工具的局限。

我已梳理完AudioReach架构的核心特征，它在Android14/15、骁龙8 Gen2及以后平台默认采用，相比老架构在HAL层、内核驱动、DSP侧、配套工具上都有全面升级，同时明确了GPR、SPF两个核心概念的定义与核心作用。SPF还负责ADSP资源分配、多Graph仲裁及参数动态管理与持久化。运行在AP侧的GSL作为ARHAL与SPF的中间层，抽象底层接口、管理Graph拓扑与共享内存。

GSL还负责接收SPF上报的各类事件并转发给ARHAL。ARHAL作为高通实现的Android AIDL Audio HAL，可将上层音频框架的标准请求转换为GSL调用，已实现三类核心AIDL接口。

补充说明ARHAL还包含音频效果相关接口，Audio Graph是AudioReach核心数据通路模型，大部分模块运行在aDSP侧，数据通过共享内存完成交互。

录音场景的Audio Graph通路已补充明确，同时梳理出了循序渐进的学习路径：先掌握Android原生音频架构，理清核心组件的作用与AIDL Audio HAL接口定义；再学习高通AudioReach的整体分层与核心概念，对比老架构明确其优势；最后从ARHAL入手，查看对应代码的目录结构与各部分实现逻辑。

我梳理了AudioReach架构下音乐播放的完整数据流链路，明确编解码环节均在DSP侧完成，接下来我将查看高通内核代码中的GPR驱动，学习AP与DSP间的通信实现逻辑。

后续的学习与实践方案我已经梳理完毕：音频相关驱动的学习可结合上游内核patch文档推进，SPF侧主要通过公开资料、头文件定义了解其核心逻辑，有条件可借助专用工具辅助调试；实践环节可基于高通开发板刷入官方镜像，通过各类工具验证音频通路、修改配置验证效果。

两类资料的获取权限要求已明确，公开资源包含官方AIDL文档、相关代码、上游内核补丁说明等，高通官方文档、开发工具等需QDN或合作伙伴权限。另外Android15对LE Audio、空间音频低延迟通路等新特性的ARHAL支持情况也已梳理完成。

我补充梳理了Android15上AudioReach的其余特性，包括低延迟音频播放、多设备音频并发、隐私审计等功能，还明确其核心优势是支持合作伙伴自定义开发DSP模块，无需修改系统固件。目前没有专门讲解AudioReach的书籍，可参考《Android音频开发》《深入理解Android音频系统》的HAL相关内容。另外我也整理了三个常见学习误区，以及仅2022年后发布的中高端高通骁龙SOC才支持该架构，老平台即使升级Android15也不会搭载AudioReach的对应说明。

选择代码分支时需注意版本匹配，Android15对应的ARHAL代码需选用LA.UM.11.x系列分支，避免出现接口兼容问题。

可参考的学习渠道包括CodeLinaro对应仓库的Issue区、邮件列表及高通OSS开源页面。AudioReach支持Linux通用系统，上游内核文档适用于Android与嵌入式Linux，若熟悉GStreamer这类图模型多媒体框架，理解其架构会更快。我将按指定的清晰分类维度，整理AudioReach相关的学习内容。</thinking>
要学习高通Android 15平台基于**AudioReach**的音频架构，首先需要明确：AudioReach是高通从2022年开始推出的**跨层端到端音频框架**，用于替代沿用多年的APM（Audio Processing Manager）旧架构，覆盖从用户空间HAL、内核驱动到aDSP/cDSP侧信号处理的全链路，Android 15要求新设备必须对接AIDL Audio HAL，而AudioReach对应的`ARHAL（AudioReach HAL）`就是高通对谷歌标准AIDL音频接口的原生实现。
以下是分层次整理的实用学习资料、架构核心概念和学习路径，所有公开资源都标注了访问方式：
---
## 一、AudioReach 核心架构总览（先建立全局认知）
在看具体资料前，先搞清楚分层逻辑（和Android 15的适配关系）：
```
┌───────────────────────────────────────────────────┐
│  App层 / Android Framework（谷歌原生，不变）       │
│  AudioTrack / MediaPlayer / AudioService          │
│  AudioFlinger / AudioPolicyService / libaudiohal  │
├───────────────────────────────────────────────────┤
│  ARHAL（高通实现，对接谷歌AIDL Audio HAL标准接口）  │
│  实现IDevice/IStream/IEffect等AIDL接口，解析上层请求│
├───────────────────────────────────────────────────┤
│  GSL（Graph Service Layer，用户空间核心库）        │
│  音频处理图(Graph)构建、模块管理、内存/事件抽象     │
├───────────────────────────────────────────────────┤
│  内核层（Linux Kernel）                            │
│  • GPR驱动：跨核IPC（替代旧的APR），传输控制命令    │
│  • ASoC AR驱动：新的音频SoC驱动，管理数据通路/DMA   │
│  • QRTR/FastRPC：底层跨核通信/共享内存机制          │
├───────────────────────────────────────────────────┤
│  DSP侧（aDSP/cDSP，核心信号处理）                   │
│  AR SPF（Signal Processing Framework）             │
│  • 动态加载音频模块（编解码、降噪、EQ、空间音频等）  │
│  • 动态构建音频Graph、调度数据流、仲裁资源          │
└───────────────────────────────────────────────────┘
```
### 关键特性（和旧架构的核心差异）
1. **全链路动态化**：音频通路拓扑不再硬编码在ADSP固件中，支持运行时动态拼接Graph（比如播放/通话/录音场景自动切换模块组合），DSP模块可动态加载（无需重新刷固件）。
2. **标准化对接**：ARHAL完全实现谷歌AIDL Audio HAL接口，替代旧的HIDL/Legacy HAL，符合Android 15强制要求。
3. **新特性原生支持**：LE Audio（Auracast/LC3）、空间音频头追低延迟、多设备并发、AI降噪等特性在SPF层原生支持。
4. **平台支持范围**：仅2022年后发布的新SOC支持，包括骁龙8 Gen2(SM8550)/8 Gen3(SM8650)/8 Gen4(SM8750)、骁龙7+ Gen2/7 Gen3、骁龙6 Gen2等，老平台（骁龙8 Gen1及以前）即使升级Android15也仍用旧APM架构。
---
## 二、官方权威学习资料
### 1. 高通官方资料（部分公开，部分需QDN账号）
最权威的架构定义和开发指南，优先看公开部分：
| 资料名称 | 访问方式 | 内容价值 |
|----------|----------|----------|
| 《AudioReach Architecture Overview》白皮书 | [QDN搜索AudioReach](https://developer.qualcomm.com/)（需注册免费QDN账号） | 架构总览、核心组件介绍、特性说明，入门必读 |
| Linaro Connect 公开演讲《Qualcomm AudioReach: An Open, Scalable Audio Framework for Android》 | [Linaro官网2023/2024 session](https://linaro.atlassian.net/wiki/spaces/LIN/pages/28642791967/Linaro+Connect+2023)、YouTube/B站可搜录像 | 高通工程师亲自讲解架构设计、和旧架构的差异、上游开源进度，有完整PPT |
| ARHAL Developer Guide / SPF Developer Guide | QDN合作伙伴专区（需厂商权限） | 详细的HAL层实现规范、DSP侧SPF接口、模块开发指南，有开发权限可直接获取 |
| AudioReach 上游内核设计文档 | [lore.kernel.org搜索AudioReach](https://lore.kernel.org/all/?q=AudioReach) | 高通提交Linux主线的驱动Patch，Cover Letter里有非常详细的内核层设计说明（GPR、ASoC AR驱动的逻辑），完全公开 |
| Qualcomm Open Source 音频项目页 | [高通开源官网](https://www.qualcomm.com/developer/open-source/projects/audio) | 所有AudioReach相关开源仓库的入口 |
### 2. 谷歌官方Android音频资料（必看基础）
ARHAL是对接谷歌标准接口的，必须先掌握Android原生音频框架和AIDL HAL规范：
- [Android 音频架构总览](https://source.android.com/docs/core/audio)：理清上层AudioFlinger、AudioPolicy的逻辑。
- [AIDL Audio HAL 官方文档](https://source.android.com/docs/core/audio/aidl)：Android15强制要求的HAL标准，ARHAL就是这个接口的高通实现，文档里有所有接口定义、数据流要求。
- AIDL接口源码路径（AOSP）：`hardware/interfaces/audio/aidl/`，可以看`IDevice.aidl`、`IStreamOut.aidl`等核心接口的定义。
---
## 三、开源代码资源（学习架构最直接的资料）
AudioReach的非DSP固件部分全部开源在CodeLinaro（原CAF，高通开源代码托管平台，无需账号即可访问），对应Android15的分支为`la.um.11.0*`系列：
### 1. 用户空间代码
| 仓库 | 地址 | 核心内容 |
|------|------|----------|
| audio-arhal | https://git.codelinaro.org/clo/la/platform/vendor/qcom/opensource/audio-arhal | ARHAL实现，直接对接AIDL接口的代码：<br>- `aidl/`：IDevice/StreamOut/StreamIn/Effect等接口的具体实现<br>- `core/`：设备路由、场景管理、参数转换逻辑<br>- 分支选择：`la.um.11.0.r1`（对应Android15） |
| audio-ar-utils | https://git.codelinaro.org/clo/la/platform/vendor/qcom/opensource/audio-ar-utils | AudioReach核心公共库：<br>- `gsl/`：Graph Service Layer实现，负责Graph的创建/销毁/配置、和DSP的交互<br>- `gpr/`：GPR IPC协议封装，打包控制命令发送到内核<br>- `osal/`：操作系统抽象层（内存、线程、锁）<br>- `inc/`：SPF的命令ID、模块ID、数据结构定义（DSP侧接口的公开头文件） |
### 2. 内核层代码（对应Android15 6.1/6.6内核）
高通Android15内核源码：https://git.codelinaro.org/clo/la/kernel/msm-6.6
核心目录：
| 路径 | 内容 |
|------|------|
| `drivers/soc/qcom/gpr/` | GPR（通用包路由）驱动实现，替代旧的APR，是AP和DSP之间控制命令的传输层 |
| `sound/soc/qcom/ar/` | AudioReach专属的ASoC驱动，管理音频数据通路、DMA、共享内存、硬件Codec配置，替代旧的`msm`系列ASoC驱动 |
| `net/qrtr/` | QRTR跨核IPC路由驱动，是GPR的底层传输机制 |
| `drivers/misc/fastrpc/` | FastRPC驱动，用于和DSP通信、动态加载SPF模块 |
> 注：DSP侧的SPF框架和核心处理模块大部分为闭源固件，但公开头文件已经包含所有接口定义，足够学习架构逻辑，自定义模块开发需要高通授权的SPF SDK。
---
## 四、第三方学习资料（适合入门理解流程）
### 1. 技术博客/文章
- 国内平台（CSDN/博客园/掘金）搜索关键词：`AudioReach 架构解析`、`高通 ARHAL 流程`、`AudioReach Graph`，有一线音频开发者整理的系列文章，比如：
  - 《高通AudioReach架构总览》：分层逻辑梳理、和旧架构对比
  - 《Android15 AIDL Audio HAL + ARHAL 播放流程全解析》：从App到DSP的完整调用链跟踪
  - 《AudioReach GSL与SPF交互机制》：Graph创建、命令下发的细节
- 国外博客：《Looking at Qualcomm's AudioReach on Linux》系列，讲解内核层驱动的实现逻辑。
### 2. 实践调试参考
- 调试工具：Android15平台上可通过`logcat -s ARHAL GSL GPR`查看用户空间日志，通过`tinymix`/`tinyplay`测试通路，QACT工具（需权限）可实时查看DSP侧的Graph拓扑、模块参数。
- CodeLinaro仓库的Issue区：audio-arhal和ar-utils的Issue里有很多开发者讨论的场景问题、适配逻辑，适合深入理解实际运行流程。
---
## 五、核心概念提前梳理（学习时重点攻克）
1. **GPR（General Packet Router）**：跨核IPC协议，替代旧的APR，支持AP↔aDSP、aDSP↔cDSP之间的可靠命令传输，所有SPF的控制命令都通过GPR包交互。
2. **SPF（Signal Processing Framework）**：运行在aDSP上的核心框架，相当于DSP侧的“音频操作系统”，负责动态加载模块、构建Graph、调度数据流、仲裁算力/内存资源。
3. **Graph（音频处理图）**：AudioReach的数据流核心模型，每个音频场景（音乐/通话/录音）对应一个Graph，由多个处理模块（Decoder/Mixer/EQ/降噪/Codec接口）通过端口连接而成，支持运行时动态拼接/销毁。
   例：音乐播放Graph：`共享内存写入端 → AAC解码模块 → 混音模块 → 音量模块 → EQ/空间音频模块 → I2S/蓝牙输出端`
4. **GSL（Graph Service Layer）**：AP侧用户空间的中间层，屏蔽SPF的底层细节，给ARHAL提供API完成Graph的生命周期管理、参数配置、内存分配。
5. **ARHAL**：高通对谷歌AIDL Audio HAL的实现层，把上层的标准请求（打开流、切设备、调音量）转换成GSL调用，最终下发到SPF执行。
---
## 六、循序渐进的学习路径建议
1. **打基础（1-2周）**：先看谷歌Android官方音频架构文档，理解AIDL Audio HAL的接口定义，知道上层AudioFlinger怎么调用HAL，不需要深入厂商逻辑。
2. **建认知（3天）**：看高通AudioReach白皮书、Linaro演讲PPT，理清分层和核心概念，搞懂GPR/SPF/GSL/Graph分别是什么，和旧APM架构的差异。
3. **啃用户空间代码（2-3周）**：
   - 先看`audio-ar-utils`里的GSL逻辑，搞懂Graph创建的流程、GPR包的封装格式；
   - 再看`audio-arhal`里的AIDL接口实现，跟踪`openOutputStream`（播放）、`openInputStream`（录音）两个核心流程，看上层请求怎么一步步转换成GSL调用。
4. **学内核层（1-2周）**：结合上游内核Patch的设计文档，看GPR驱动和ASoC AR驱动的逻辑，理解控制命令怎么从用户空间传到DSP，音频数据怎么通过共享内存传输。
5. **实践调试**：如果有骁龙8 Gen2/Gen3的开发板/量产机刷Android15，可通过日志跟踪实际播放场景的Graph创建流程，有QACT权限可直接查看DSP侧的拓扑结构，验证所学逻辑。
---
## 七、Android15上AudioReach的特殊适配点
Android15新增的音频特性全部由ARHAL+SPF原生支持：
- 强制AIDL HAL v7版本，ARHAL完全兼容最新的音量分组、设备路由、音频策略扩展；
- LE Audio Auracast广播、多流音频的低延迟通路，SPF原生集成LC3编解码和蓝牙路由模块；
- 空间音频头追的传感器数据直连DSP通路，延迟低于20ms；
- 多设备并发输出（同时连多个蓝牙/有线设备），SPF支持单Graph多Sink路由，不需要AP侧混音；
- 隐私录音审计、音频焦点增强等框架特性的硬件级支持。

<thinking>针对用户询问的ARHAL与PAL的区别，我已梳理清楚二者的核心差异：二者分属高通新旧两代音频架构，PAL是旧APM音频架构的用户空间中间层，ARHAL是新AudioReach架构中对接谷歌标准接口的HAL实现，二者不在同一架构栈，不存在同时运行的可能。

我进一步明确了二者的核心定位：PAL是高通传统APM音频架构下的用户空间中间适配层，仅适配旧版Android HAL接口；ARHAL是新AudioReach架构下直接实现谷歌官方AIDL Audio HAL标准接口的HAL实现，二者分属不同代际的架构体系。

针对两者的对比维度已经梳理完成，同时明确了新旧架构的完整分层链路，新架构中ARHAL与GSL的组合功能基本对应旧架构里旧HAL实现加PAL的总和。可以明确的是，ARHAL并非PAL的直接替代品，新架构的ARHAL+GSL对应旧架构的旧HAL实现+PAL功能组合，后续我会厘清二者的定位差异。第一个核心差异已明确：二者定位与分层位置完全不同。PAL是旧架构的私有中间适配层，不直接实现谷歌Audio HAL接口，上层还有薄HAL；ARHAL是新架构的标准HAL实现层，直接实现谷歌AIDL Audio HAL接口，属于谷歌定义的官方HAL实现，无额外上层壳。

PAL对接的上下层与协议标准：上层为高通私有HAL内部接口，与谷歌标准无关；下层对接旧内核相关驱动及DSP侧APM框架，通路配置硬编码。

接下来补充其余几项核心差异：
ARHAL上层对接谷歌官方定义的AIDL Audio HAL v6/v7接口，完全符合谷歌CDD要求，无私有扩展，上层可直接调用无需适配层；下层对接GSL公共API，支持动态构建音频拓扑，无需硬编码使用场景。
设计目标上，PAL核心是做平台抽象，实现音频代码跨平台复用，仅支持预定义使用场景、设备路由、参数转换，无法动态构建通路；ARHAL核心是符合谷歌标准、对接新的AudioReach动态音频模型，可动态拼接任意通路，支持多设备并发、LE Audio等全量谷歌要求的音频功能，所有扩展均通过标准AIDL接口实现。
灵活性方面，PAL的所有音频通路拓扑均为提前写死，调整或新增场景需修改代码、配置甚至重新编译固件；ARHAL可动态构建对应通路，无需预定义使用场景，新增功能不用改固件，适配成本低。
和DSP侧对接上，PAL对接旧的静态APM框架，仅需发送场景ID即可触发对应固定通路加载。

ARHAL对接DSP侧的动态框架SPF，支持模块动态加载与实时通路构建。PAL时代HAL版本升级时仅需调整上层薄HAL，厂商需自行修改大量PAL代码，适配成本高且易出现兼容性问题。

ARHAL适配维护成本更低，跨平台复用率更高且兼容性更好，仅支持骁龙8 Gen2及以后的新平台、Android14及以上系统版本，Android15强制要求使用。

我补充了几项关键的架构差异：ARHAL下的GSL是轻量纯图形抽象层，仅负责图形生命周期管理等基础功能，与功能更全面的PAL完全不同；旧架构PAL会承担部分AP侧音频软处理，新架构中这类逻辑已迁移至DSP SPF或ARHAL的标准处理库，GSL不参与相关处理。此外，旧架构音频效果与主通路分离，易出现同步问题，新架构效果模块已整合进主链路，同步性大幅提升，我还梳理了新旧架构下音乐播放流的完整处理流程差异。

补充两点信息：多设备并发场景下，PAL需提前定义特殊usecase且依赖固件支持，ARHAL可动态构建更灵活；采用AudioReach架构的平台已完全移除PAL，Android15的对应代码仓库已替换为ARHAL相关组件，旧Android13分支仍保留PAL仓库。

补充对比了PAL与ARHAL的接口属性及测试要求差异，同时明确了两者属于不同世代的架构组件，并非替代关系，我将按指定框架整理相关内容。还需补充两者的代码路径差异，旧架构PAL、新架构ARHAL及GSL的代码存放位置不同，新代码无PAL相关路径，同时要注意区分易混淆的术语缩写。

补充了两者资源管理逻辑的差异：PAL采用硬编码资源分配与仲裁，ARHAL则由SPF侧动态分配，更高效。同时明确了二者的核心区别：ARHAL与分属高通两代不同音频架构，不存在同时运行的可能，旧架构为PAL+薄HAL，新架构替换为ARHAL+GSL，二者定位、分层与设计逻辑均有本质差异。

我补充了新旧音频架构的分层结构、核心差异对比表，澄清了两个常见认知误区：一是GSL并非新架构中的PAL，二者设计逻辑完全不同；二是ARHAL并非PAL的升级版，二者是架构换代后的不同组件，旧架构为「薄HAL+重PAL」的私有设计，新架构为「重ARHAL（标准化）+轻GSL（抽象化）」的标准化设计。目前我已梳理完音乐播放场景下旧架构的部分流程。

我补充了新旧架构的实际流程示例，旧PAL架构下的音乐播放流程完全依赖硬编码的预定义通路，新增或调整功能需要修改对应代码、更新固件；新ARHAL架构可在运行时动态拼接模块实现双设备输出、自定义效果链等复杂场景。目前Android15的高通平台已彻底移除PAL相关代码，全链路统一采用AudioReach架构。</thinking>
ARHAL 和 PAL 是高通**两代完全不同音频架构中的组件，不存在同时运行的情况，定位、分层、设计逻辑有本质区别**，核心结论可以先明确：
- PAL 是高通**旧APM音频架构（2022年前，骁龙8 Gen1及以前平台，Android13及以前版本）**的「厂商私有中间适配层」，位于谷歌标准HAL的下方，不直接对接谷歌接口，属于厂商内部实现逻辑。
- ARHAL 是高通**新AudioReach音频架构（2022年后，骁龙8 Gen2及以后平台，Android14/15默认）**的「谷歌AIDL Audio HAL标准实现本身」，直接对接上层AudioFlinger，是谷歌定义的音频HAL层的官方实现，不是中间件。

简单来说：旧架构里「薄HAL壳 + PAL」两层的组合，在新架构里被「ARHAL + GSL（Graph Service Layer）」替代——ARHAL承担了旧架构中HAL壳的全部职责+部分PAL的上层逻辑，GSL承担了类似PAL的底层适配职责，但设计思路完全不同。
---

## 一、两者在各自架构中的位置对比
先通过分层栈直观理解差异：
### 旧APM架构（含PAL）分层（从上到下）
```
┌──────────────────────────────────┐
│ Android Framework（AudioFlinger）│
├──────────────────────────────────┤
│ 高通HIDL/Legacy Audio HAL（薄壳）│ ← 仅做谷歌接口适配，无核心业务逻辑
├──────────────────────────────────┤
│ PAL（Platform Abstraction Layer）│ ← 核心逻辑层，所有音频场景处理都在这里
│ 硬编码usecase、路由、参数转换、适配│
├──────────────────────────────────┤
│ 内核APR/旧MSM ASoC驱动           │
├──────────────────────────────────┤
│ DSP侧APM（静态通路管理框架）      │
└──────────────────────────────────┘
```
### 新AudioReach架构（含ARHAL）分层（从上到下）
```
┌──────────────────────────────────┐
│ Android Framework（AudioFlinger）│
├──────────────────────────────────┤
│ ARHAL（AudioReach HAL）          │ ← 厚实现，直接完成谷歌AIDL HAL的所有逻辑
├──────────────────────────────────┤
│ GSL（Graph Service Layer）       │ ← 轻量Graph抽象层，无硬编码业务逻辑
├──────────────────────────────────┤
│ 内核GPR/新AR ASoC驱动            │
├──────────────────────────────────┤
│ DSP侧SPF（动态信号处理框架）      │
└──────────────────────────────────┘
```
---
## 二、核心差异详细对比
| 对比维度                | PAL（旧架构）                                                                 | ARHAL（新架构）                                                                 |
|-------------------------|------------------------------------------------------------------------------|--------------------------------------------------------------------------------|
| 本质定位                | 高通私有中间适配层，对谷歌透明，属于厂商内部实现，无标准化约束                 | 谷歌AIDL Audio HAL标准的官方实现，是Android音频HAL层本身，属于系统标准化组件     |
| 分层位置                | 旧HAL壳下方、内核驱动上方，上层无直接对接系统框架的能力                       | 直接对接上层AudioFlinger，是厂商侧音频栈的最顶层，通过Binder和系统框架直接通信   |
| 上层对接协议            | 对接高通私有HAL内部接口，和谷歌标准完全无关，接口可以随意修改                 | 对接谷歌公开的AIDL Audio HAL v6/v7接口（Android14/15），严格符合CDD/CTS/VTS要求 |
| 下层对接组件            | 旧APR跨核驱动、DSP侧静态APM框架，通路为预定义硬编码拓扑                       | GSL抽象库、GPR跨核驱动、DSP侧动态SPF框架，通路为运行时动态构建的Graph拓扑        |
| 核心设计思路            | 「静态预定义」：所有音频场景以`usecase`形式硬编码，通路拓扑提前写死在固件/配置里 | 「动态标准化」：对齐谷歌接口规范，音频通路以Graph形式运行时拼接，无需预定义场景   |
| 核心能力                | 1. 匹配预定义usecase选择固定通路<br>2. 设备路由、ALSA kcontrol映射<br>3. 参数转APR命令<br>4. 硬编码多场景资源仲裁 | 1. 完整实现谷歌所有HAL接口（流管理、路由、音量、效果、LE Audio/空间音频等）<br>2. 处理谷歌标准音频策略请求<br>3. 将上层请求转换为GSL的Graph操作<br>4. 支持谷歌标准化扩展接口 |
| 灵活性与扩展性          | 极低：新增场景/调整通路必须修改PAL代码、更新XML配置，甚至重刷DSP固件           | 极高：任意通路可动态拼接，DSP模块可运行时加载，无需预定义场景，原生支持多设备并发、自定义效果链等复杂场景 |
| 代码路径（CodeLinaro）  | `vendor/qcom/opensource/audio-pal`（Android13及以前分支存在）                 | `vendor/qcom/opensource/audio-arhal`（Android14及以后分支存在）                 |
| 兼容性保障              | 仅上层薄HAL接受VTS测试，PAL逻辑无标准化约束，厂商随意修改容易导致兼容性问题     | ARHAL直接作为HAL实现接受VTS/CTS测试，接口标准化，兼容性有谷歌强制规范约束         |
| 支持平台与系统版本      | 骁龙8 Gen1(SM8450)及以前平台，Android13及以前版本                             | 骁龙8 Gen2(SM8550)及以后平台，Android14及以后版本（Android15强制要求）           |
---
## 三、常见误区澄清
### 1. 「GSL就是新架构里的PAL」？完全错误
GSL是ARHAL下方的轻量抽象层，仅负责三个基础能力：Graph生命周期管理、GPR跨核命令封装、跨核共享内存抽象，**没有任何硬编码的usecase判断、路由逻辑、资源仲裁规则**——这些业务逻辑要么在ARHAL侧（符合谷歌标准的部分），要么下沉到DSP侧SPF动态处理，和旧PAL大而全、硬编码所有业务逻辑的设计完全不同。
### 2. 「ARHAL是PAL的升级版」？不准确
两者不是垂直迭代关系，而是架构换代后的不同组件：
- 旧架构「薄HAL + 重PAL」的设计目标是「高通内部跨平台代码复用」，不需要考虑谷歌标准化要求，灵活性差、适配成本高。
- 新架构「重ARHAL（标准化） + 轻GSL（抽象化）」的设计目标是「符合谷歌强制AIDL HAL规范+动态音频能力」，既满足Android版本兼容性要求，又支持新音频特性的快速迭代。
---
## 四、实际流程对比（音乐播放场景）
### 旧架构（PAL）流程
1. AudioFlinger调用HIDL HAL的`openOutputStream`传入播放参数；
2. 薄HAL仅做参数透传，调用PAL私有API `pal_open_stream(USECASE_AUDIO_PLAYBACK_MUSIC, ...)`，指定提前写死的音乐场景ID；
3. PAL根据硬编码的usecase匹配预定义通路，配置ALSA kcontrol，打包APR命令通知DSP侧APM加载固定的音乐通路模块（解码、混音、输出等都是固件编译时打包好的）；
4. APM返回通路句柄，PAL逐层透传给上层。
> 如果要支持音乐同时输出到有线耳机+蓝牙耳机，必须提前在PAL里新增`USECASE_AUDIO_PLAYBACK_DUAL_DEVICE`的硬编码逻辑，且APM固件提前写死双通路拓扑，否则根本无法实现。
### 新架构（ARHAL）流程
1. AudioFlinger调用AIDL IDevice的`openOutputStream`，传入标准`AudioAttributes`、采样率、输出设备列表；
2. ARHAL直接处理请求，根据参数解析需要的模块（解码、混音、EQ、输出端点），调用GSL接口创建Graph，如果是双设备输出就动态添加两个输出端点模块，按需求连接拓扑；
3. GSL打包GPR命令通知DSP侧SPF动态加载所需模块（不需要提前打包在固件里）、构建通路、分配共享内存；
4. SPF返回Graph句柄，ARHAL直接返回AIDL `IStreamOut` Binder对象给上层。
> 双设备输出、自定义AI降噪效果链、空间音频动态接入等场景不需要提前预定义，运行时动态拼接Graph即可实现。
---
## 补充说明
在Android15的高通新平台代码中，`audio-pal`仓库已经被完全移除，所有旧APM/PAL相关的代码都不再保留，全链路统一使用ARHAL+GSL+SPF的AudioReach架构，PAL已经彻底退出新平台的音频栈。